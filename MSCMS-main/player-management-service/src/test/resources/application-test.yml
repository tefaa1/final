spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: embarkx
    password: embarkx
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  test:
    database:
      replace: none

